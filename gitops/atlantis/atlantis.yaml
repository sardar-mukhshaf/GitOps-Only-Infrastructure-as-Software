version: 3
projects:
  - name: infrastructure
    dir: terraform
    workspace: default
    terraform_version: v1.7.0
    autoplan:
      when_modified: ["*.tf", "**/*.tf", "../modules/**/*.tf"]
      enabled: true
    apply_requirements: [approved, mergeable]
    workflow: strict
