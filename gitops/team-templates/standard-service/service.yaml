apiVersion: v1
kind: Service
metadata:
  name: standard-service
  annotations:
    description: "Standard service exposed via ClusterIP"
    managed-by: "gitops"
spec:
  selector:
    app: standard-service
  ports:
    - port: 80
      targetPort: 8080
  type: ClusterIP
